# 项目开发日志

## 2026-05-08 23:44 +08:00

### 本次目标

- 在 1 小时内完成《一起A了吧》的本地可运行核心原型。
- 所有项目文件放在 `C:\Users\Administrator\Desktop\自娱自乐的AI\Codex`。
- 优先保证能录入家庭/成员/费用，并能看到 AA 结算结果。

### 已确认环境

- 可用：`node v24.14.0`
- 不可用：`npm`、`pnpm`、`yarn`、`corepack`

### 执行策略

- 由于当前环境没有 npm，暂不创建 React + Vite + Tailwind 工程。
- 本次改做零构建静态原型，使用原生 HTML/CSS/JavaScript 和 IndexedDB。
- 保留清晰的业务函数，后续可以迁移到 React + TypeScript。

### 本轮计划

- 创建入口文件、样式文件、核心脚本和本地静态服务器。
- 实现 IndexedDB 保存。
- 实现家庭结算、部分人 AA、充值赠送折算、忽略费用。
- 实现简洁总结、详细总结、JSON 导出。

### 暂不做

- React + Vite + Tailwind 工程化。
- 完整 5 页面路由。
- 搜索筛选。
- 常用成员推荐。
- 完整导入校验。
- 自动化测试。

## 2026-05-08 23:52 +08:00

### 已完成

- 创建 `index.html`、`styles.css`、`app.js`、`server.mjs`。
- 实现零构建静态应用入口。
- 实现 IndexedDB 读取和保存，数据库名为 `yiqi-aa-ledger`。
- 实现示例账本，打开页面即可看到一组露营费用试算。
- 实现核心结算函数：
  - 计入 AA 总金额
  - “这笔先不 A”
  - 按家庭 A
  - 按人头 A
  - 只和几个人 A
  - 充值赠送折算
  - 主结算建议
  - 家庭内部参考结算
- 实现 JSON 导出和基础 JSON 导入。
- 实现单页核心界面：活动设置、家庭/成员、费用录入、费用记录、结算结果。

### 仍未完成

- 没有 React + TypeScript + Vite + Tailwind 工程化。
- 没有编辑已有费用，只支持新增和删除。
- 没有搜索、分类筛选、付款人筛选。
- 没有常用成员/家庭推荐。
- 导入校验只做基础结构检查。
- 结算逻辑还没有自动化测试覆盖。

### 明天建议优先做

- 迁移到 React + TypeScript + Vite + Tailwind。
- 把 `app.js` 拆成 `types`、`storage`、`settlement`、`components`。
- 为结算函数补单元测试。
- 增加费用编辑、搜索筛选和更完整的移动端交互。

## 2026-05-08 23:58 +08:00

### 验证结果

- `node --check .\app.js` 通过。
- `node --check .\server.mjs` 通过。
- 已启动本地静态服务。
- `http://localhost:4173` 返回 HTTP 200。

### 当前可访问地址

- `http://localhost:4173`

### 下次继续时先看

- `README.md`
- `项目开发日志.md`
- `app.js` 中的 `calculateSettlement()`、`compactSummary()`、`detailedSummary()`

## 2026-05-09 00:06 +08:00

### 已继续完成

- 按用户要求补齐项目内开发环境。
- 下载并解压官方便携 Node 包到 `.tools/node-v24.14.0-win-x64`。
- 当前项目内可用：
  - Node `v24.14.0`
  - npm `11.9.0`
- 安装 React + TypeScript + Vite + Tailwind CSS + Vitest。
- 将应用迁移为正式 Vite 工程。
- 旧静态原型已移动到 `legacy-static/`，可作为回退参考。
- 新增源码目录 `src/`：
  - `types.ts`：核心数据类型
  - `constants.ts`：活动类型、类别、文案映射
  - `utils.ts`：金额、日期、下载工具
  - `sample.ts`：示例账本和创建函数
  - `settlement.ts`：结算、总结文案、充值赠送折算
  - `storage.ts`：IndexedDB 保存/读取和导入校验
  - `App.tsx`：React 单页界面
  - `settlement.test.ts`：结算测试

### 新增功能

- 费用编辑。
- 搜索费用。
- 按类别筛选。
- 按付款人筛选。
- 更严格的 JSON 导入基础校验。
- 结算逻辑 Vitest 测试。

### 验证结果

- `npm test` 通过：1 个测试文件，4 个测试全部通过。
- `npm run build` 通过。
- Vite 开发服务已启动。
- `http://127.0.0.1:5173/` 返回 HTTP 200。

### 当前可访问地址

- React/Vite 版：`http://127.0.0.1:5173`
- 旧静态版文件：`legacy-static/`

### 后续还剩

- 做完整 5 页面路由。
- 做常用成员/家庭推荐。
- 继续精修移动端录入体验。
- 增加 Playwright 或端到端测试。
- 补更多结算边界测试，例如无家庭成员、单家庭、多付款人、多组选择。

## 2026-05-09 00:15 +08:00

### 已继续完成

- 增加应用内 5 页面导航：
  - 活动列表
  - 活动设置
  - 费用列表
  - 新增/编辑费用
  - 结算结果
- 桌面端顶部导航、手机端底部导航已接入。
- 活动列表页已可查看当前账本、创建空白活动、导入/导出 JSON。
- 新增常用家庭/成员组合：
  - 可把当前家庭/成员保存为常用组合。
  - 可从常用组合快速添加家庭和成员。
  - 常用组合保存在浏览器 `localStorage`。
- 新增导入校验测试 `src/storage.test.ts`。

### 验证结果

- `npm run build` 通过。
- `npm test` 通过：2 个测试文件，8 个测试全部通过。

## 2026-05-09 01:31 +08:00

### 根据继续试用反馈优化

- 活动列表改为真正保存多个活动记录：
  - 每个活动账本以 `activity:{activityId}` 存入 IndexedDB。
  - 保留旧版 `current` 单账本兼容迁移。
  - 活动列表可切换、编辑、移除历史活动。
- 创建新活动不再覆盖之前活动，之前活动会留在活动列表。
- 新增家庭时自动创建 3 位大人成员：
  - 例如新增“王五家”，自动生成“王五家大人1/2/3”。
  - 后续仍可手动继续增加或移除成员。
- 记账日期默认改为活动开始日期，没有开始日期才用今天。
- 合并“记账”和“费用”模块：
  - 顶部和底部导航移除单独“记账”入口。
  - 费用页左侧是记账表单，右侧是费用记录。
- 常用家庭/成员增加管理能力：
  - 改名
  - 用当前家庭成员更新
  - 移除
  - 点击名称快速添加到当前活动
- 常用家庭/成员继续按“家庭名 + 成员名”去重，避免重复叠加。

### 验证结果

- `npm run build` 通过。
- `npm test` 通过：2 个测试文件，8 个测试全部通过。
- `http://192.168.1.17:5173/` 返回 HTTP 200。

## 2026-05-09 01:36 +08:00

### GitHub Pages / HTTPS PWA 部署准备

- 查询 GitHub Pages 官方文档后，采用 GitHub Actions 发布方式。
- 新增 `.github/workflows/pages.yml`：
  - checkout
  - setup node
  - `npm ci`
  - `npm test`
  - `npm run build`
  - upload Pages artifact
  - deploy Pages
- 新增 `.gitignore`，排除：
  - `node_modules/`
  - `.tools/`
  - `dist/`
- GitHub Pages 子路径适配：
  - `vite.config.ts` 增加 `base: "./"`。
  - `index.html` 的 manifest/icon 改为相对路径。
  - `manifest.webmanifest` 的 `start_url`、`scope`、icons 改为相对路径。
  - `src/pwa.ts` 按 Vite `BASE_URL` 注册 `sw.js`。
  - `public/sw.js` 按 `self.registration.scope` 计算缓存路径，适配 `https://用户名.github.io/仓库名/`。

### 验证结果

- `npm run build` 通过。
- `npm test` 通过：2 个测试文件，8 个测试全部通过。
- 检查 `dist/index.html`：资源路径为 `./assets/...`。
- 检查 `dist/manifest.webmanifest`：`start_url` 和 `scope` 为 `./`。
- `http://127.0.0.1:5173/` 返回 HTTP 200。
- 尝试使用 in-app browser 自动化验证，但当前 browser-use 后端未发现可连接的 IAB 实例；未阻塞本地 HTTP 和构建验证。

### 当前仍剩

- 多活动历史列表：目前活动列表页展示的是当前本地账本，不是真正的多活动数据库。
- 更细的移动端录入体验，例如底部固定“记一笔”、表单分步、常用类别快捷入口。
- 端到端测试：需要 browser-use/Playwright 环境正常后再补。
- 更多结算边界测试：单家庭、无家庭成员、仅部分家庭参与、多笔付款人互相抵消。

## 2026-05-09 00:27 +08:00

### 手机访问空白页修复

- 现象：手机通过 `http://192.168.1.17:5173` 可以打开页面，但页面为空白。
- 判断：局域网 HTTP 地址不是安全上下文，部分手机浏览器不支持 `crypto.randomUUID()`，应用初始化阶段生成示例账本时会报错。
- 修复：`src/utils.ts` 中的 `uid()` 增加兼容 fallback：
  - 优先使用 `crypto.randomUUID()`
  - 不可用时使用时间戳 + `Math.random()`

### 验证结果

- `npm run build` 通过。
- `npm test` 通过：2 个测试文件，8 个测试全部通过。
- `http://192.168.1.17:5173/` 返回 HTTP 200。

## 2026-05-09 00:33 +08:00

### PWA 改造

- 新增 `public/manifest.webmanifest`。
- 新增 PWA 图标：
  - `public/icons/icon.svg`
  - `public/icons/icon-192.png`
  - `public/icons/icon-512.png`
- 新增 `public/sw.js`。
- 新增 `src/pwa.ts` 并在 `src/main.tsx` 注册 Service Worker。
- `index.html` 增加 manifest、theme color、description、icon、apple touch icon。
- 页面增加离线状态提示：
  - HTTPS 或 localhost 下尝试注册离线能力。
  - 局域网 HTTP 下提示“可正常试用；要安装离线应用，需要通过 HTTPS 地址打开”。
- `sw.js` 安装时会缓存应用壳、图标，并解析生产构建的 `/assets/` 资源加入缓存。
- `npm run preview` 改为默认监听 `0.0.0.0`，方便局域网预览生产构建。

### 重要限制

- 手机通过 `http://192.168.1.17:5173` 可以试用网页，但多数手机浏览器不允许在普通局域网 HTTP 地址上注册 Service Worker。
- 真正“电脑关闭后手机也能打开”的 PWA 需要 HTTPS 地址。
- 推荐把 `dist/` 部署到 Vercel、Netlify、GitHub Pages 或自己的 HTTPS 静态服务器。

### 验证结果

- `npm run build` 通过。
- `npm test` 通过：2 个测试文件，8 个测试全部通过。
- `dist/` 已包含 `manifest.webmanifest`、`sw.js` 和图标文件。

## 2026-05-09 01:13 +08:00

### 根据手机端试用反馈优化

- 统一把用户可见文案里的“家庭/小组”改成“家庭”。
- “显示家庭内部参考”改为“显示家庭内参考分摊”，并增加解释：
  - 只给每个家庭自己看。
  - 例如夫妻之间谁多垫了多少。
  - 不影响家庭之间主结算。
- 修复原生 checkbox 被全局输入框样式撑得过大的问题。
- 记账页改成左侧记账、右侧费用列表。
- 费用表单里“使用活动默认规则”增加当前默认规则说明，减少理解成本。
- 新增成员表单改为受控输入：
  - 添加成员后保留上一次选择的家庭。
  - 避免后续连续添加成员时误分到别的家庭。
- 费用付款人自动兜底：
  - 如果当前付款人不存在，会自动选第一位可付款成员。
  - 没有可付款成员时给出明确提示。
- 常用家庭/成员添加时按“家庭名 + 成员名”去重，避免重复叠加。
- 家庭和成员模块增加“参与活动的成员”区域，成员可直接移除。
- 字体栈增加 `PingFang SC`、`HarmonyOS Sans`、`MiSans`，移动端观感更接近原生应用。

### 验证结果

- `npm run build` 通过。
- `npm test` 通过：2 个测试文件，8 个测试全部通过。
## 2026-05-09 02:24:37 +08:00

### 后续修改与同步 GitHub 方法

- 用户以后想修改功能、界面、文案或修复问题时，可以直接告诉 Codex 修改需求。
- Codex 应优先在本地项目目录 `C:\Users\Administrator\Desktop\自娱自乐的AI\Codex` 修改代码。
- 修改完成后必须先运行验证：
  - `npm test`
  - `npm run build`
- 如果当前 PowerShell 找不到 `npm`，使用项目内便携 Node：
  - `$env:Path = "C:\Users\Administrator\Desktop\自娱自乐的AI\Codex\.tools\node-v24.14.0-win-x64;" + $env:Path`
- 验证通过后执行 Git 同步：
  - `git status --short`
  - `git add .`
  - `git commit -m "简短说明本次修改"`
  - `git push origin main`
- 推送后 GitHub Actions 会自动部署到 GitHub Pages。
- 部署完成后检查：
  - GitHub Actions 最新 `Deploy to GitHub Pages` 是否成功。
  - `https://shaocong1987-collab.github.io/yiqi-aa/` 是否返回 HTTP 200。
- 每次较大的修改都要继续更新 `项目开发日志.md`，记录：
  - 日期时间
  - 已完成
  - 未完成
  - 后续剩余工作
  - 已知问题或优化建议

### 当前产品后续可优化项

- 手机端真实验证 PWA：
  - 添加到主屏幕。
  - 首次联网打开后断网再打开。
  - 检查离线状态提示是否清楚。
- 增强费用编辑能力：
  - 支持修改已录入费用。
  - 支持复制一笔费用快速新建。
  - 支持按付款人、家庭、日期筛选。
- 优化结算体验：
  - 增加“一键复制结算文案”。
  - 增加“按家庭汇总”和“按人汇总”的切换。
  - 对复杂场景给出更容易理解的解释。
- 优化家庭和成员管理：
  - 支持拖拽或排序成员。
  - 支持成员备注，例如“大人/孩子/老人/朋友”。
  - 常用家庭成员可增加更明确的覆盖/合并提示。
- 数据安全与迁移：
  - 增加手动备份和恢复入口。
  - 增加导入失败时的详细错误提示。
  - 考虑增加本地数据版本升级说明。
- UI 继续打磨：
  - 进一步压缩移动端页面层级。
  - 减少不必要的说明文字。
  - 优化按钮密度、表单间距、费用列表可读性。
- 代码工程化：
  - 将 `src/App.tsx` 拆成更小的组件。
  - 增加更多核心业务测试，尤其是多人付款、部分人 A、家庭内参考分摊。
  - 增加简单的端到端冒烟测试。

### 今日任务收尾

- GitHub Pages 404 已解决。
- 项目已成功部署到 `https://shaocong1987-collab.github.io/yiqi-aa/`。
- 后续可直接通过 Codex 本地修改、测试、提交、推送、自动部署。

## 2026-05-09 02:20:55 +08:00

### 已完成

- 将本地完整项目初始化为 Git 仓库，并绑定远程仓库 `https://github.com/shaocong1987-collab/yiqi-aa.git`。
- 提交并推送完整源码到 GitHub `main` 分支。
- 远程仓库现已包含 `.github/workflows/`、`src/`、`public/`、`legacy-static/` 等目录。
- GitHub Actions 最新一次 `Deploy to GitHub Pages` 已成功。
- `https://shaocong1987-collab.github.io/yiqi-aa/` 已返回 HTTP 200，之前的 404 已解决。

### 未完成

- 尚未在手机端实际验证 PWA 添加到主屏幕与断网离线打开。

### 后续剩余工作

- 手机访问 `https://shaocong1987-collab.github.io/yiqi-aa/`，等待页面提示离线访问准备完成。
- Android Chrome 使用“添加到主屏幕/安装应用”；iPhone Safari 使用“分享 -> 添加到主屏幕”。
- 关闭网络或进入飞行模式后，从手机主屏幕图标打开，验证离线可用。

## 2026-05-09 02:17:51 +08:00

### 已完成

- 重新检查 GitHub 仓库 `shaocong1987-collab/yiqi-aa`，确认 Pages 404 的主要原因是仓库缺少 `src/`、`public/`、`.github/` 等关键目录，且根目录存在错误的 `pages.yml`。
- 使用 winget 安装 Git for Windows，并在重启 Codex 后确认 `git version 2.54.0.windows.1` 可用。
- 本地重新运行 `npm test`，2 个测试文件、8 个测试全部通过。
- 本地重新运行 `npm run build`，Vite 生产构建通过。
- 初始化本地 Git 仓库，准备将完整项目源码推送到 `https://github.com/shaocong1987-collab/yiqi-aa.git`。

### 未完成

- 尚未完成 GitHub 推送。
- 尚未确认 GitHub Actions 部署是否变绿。
- 尚未确认 `https://shaocong1987-collab.github.io/yiqi-aa/` 是否可以正常打开。

### 后续剩余工作

- 提交完整项目源码。
- 推送到 GitHub `main` 分支，并移除远程仓库根目录错误的 `pages.yml`。
- 等待 GitHub Actions `Deploy to GitHub Pages` 成功。
- 用手机访问 GitHub Pages HTTPS 地址，确认 PWA 可安装和离线打开。
